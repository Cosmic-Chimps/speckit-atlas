# Feature Specification: With Code

**Status**: Implemented

A feature whose artifacts name the source files that fulfill it, in the forms real
Spec Kit repos use. See a helper at [elements](../../../../src/webview/map/elements.ts).
